{
  "_why": [
    "THE SINGLE SOURCE for which client event each hook role handles.",
    "",
    "Sprint 28, C5 audit finding F5. The role table asserted the COUNTS 6 and 4",
    "and nothing else, so nine of the ten event names had no discriminator: rename",
    "afterMCPExecution in the deploy, leave roles.rs alone, and both crates stay",
    "green while the observer maps to an event Cursor never fires. That is the",
    "ten-scripts drift the table was built to end, reproduced inside the table.",
    "",
    "A shared constant is impossible: jawata-hook must not depend on the studio",
    "(a hook process must never link a GUI toolkit) and the studio must not depend",
    "on the hook (deploy writes the binary, it never runs it). Both forbidden edges",
    "are asserted by tests. A committed data file is the one thing both sides can",
    "read without an edge between them — each includes it at compile time and",
    "asserts its own table against it.",
    "",
    "Change an event here and BOTH sides fail until they agree.",
    "",
    "Sprint 28, C8 architect finding F2. The same argument now covers the FILES",
    "that cross the boundary, not only the event names. The hook appends to",
    "hook_silence.log and the studio renames it away — two processes, two crates,",
    "one file — and that seam was declared NOWHERE. It had already produced three",
    "hand-copied facts (the cap, the filename, the rotated name) with nothing",
    "asserting they agree, plus an ordering contract living only in a private doc",
    "comment. Same fix, same file: declare it once, assert from both sides."
  ],
  "claude-code": {
    "primer": "SessionStart",
    "userprompt": "UserPromptSubmit",
    "recall": "PreToolUse",
    "guard": "PreToolUse",
    "observer": "PostToolUse",
    "stop": "Stop"
  },
  "cursor": {
    "primer": "sessionStart",
    "userprompt": "beforeSubmitPrompt",
    "guard": "beforeShellExecution",
    "observer": "afterMCPExecution"
  },
  "_absent": {
    "cursor.recall": "Cursor fires no before-a-tool-call event; afterMCPExecution is the only tool-shaped hook and it runs too late to recall INTO the call",
    "cursor.stop": "Cursor has no agent-stop event, so the stop gate cannot exist there; the discipline it enforces is carried by the rule block instead"
  },
  "seam_files": {
    "_why": "Every file that crosses the hook/studio boundary. One row each: who writes, who reads, who bounds it, and what a violation looks like.",
    "hook_config.json": {
      "writer": "studio",
      "reader": "hook",
      "lifetime": "install",
      "atomicity": "temp file then rename",
      "violation": "a hook reads a zero-length file and reports a torn deploy"
    },
    "hook_silence.log": {
      "writer": "hook",
      "reader": "human",
      "rotator": "studio",
      "lifetime": "install",
      "atomicity": "one write_all under O_APPEND",
      "max_bytes": 262144,
      "hard_ceiling_multiple": 8,
      "rotated_name": "hook_silence.log.1",
      "retention_generations": 1,
      "bounding": "the STUDIO rotates; the hook only appends and drops past the ceiling",
      "violation": "a hook that trims, truncates or renames this file — six audit rounds proved in-path bounding destroys records"
    }
  },
  "hook_budget": {
    "_why": [
      "The studio's store-slow line IS the hook's effective store budget, and the two",
      "live in different crates that MUST NOT link (see dependency_edges.rs: deploy",
      "writes the hook binary, it never runs it, so an edge would rebuild the GUI on a",
      "hook change). Repeating the number on both sides made the studio's test assert",
      "1500 - 300 == 1200 and nothing else: the C5 audit changed STDIN_DEADLINE to 4000",
      "and the whole studio suite stayed green while the tile went on calling a store",
      "healthy that the hook could no longer use.",
      "",
      "So the number lives HERE, in the file both crates already include_str!, and each",
      "side asserts against it. Same shape as stop_rules: one fact, two readers, neither",
      "linking the other.",
      "",
      "stdin_deadline_millis - budget_margin_millis is what a recall actually gets. Above",
      "it, every recall on the machine is timing out silently while a canary that buys",
      "ten seconds reports health."
    ],
    "stdin_deadline_millis": 1500,
    "budget_margin_millis": 300,
    "store_slow_millis": 1200
  },
  "stop_rules": {
    "_why": [
      "Sprint 28, C8 architect finding F3. TWO stop gates exist: the deployed bash",
      "script (live today) and the Rust stop::judge (built, inert). Their rule sets",
      "have DIVERGED — the audit-fix-loop trigger and the unjudged-ask check were",
      "added to the bash generation, which the architecture schedules for deletion.",
      "At cutover the binary would ship WITHOUT rules the script had: the hook-outage",
      "shape inverted, at the one moment nobody is reading the old file.",
      "",
      "Every rule is declared here with its status on each side. A rule may be",
      "ported or deliberately dropped — but not silently lost.",
      "",
      "Sprint 28 dogfood, first probe after the release: the six role binaries were",
      "DEPLOYED and every client event still invoked the .sh script — the deploy",
      "writes binaries, then writes settings entries pointing at scripts, and no code",
      "path builds a command aimed at a binary. Pointing a client at the binary then",
      "would have STRIPPED five working protections to gain two inert ones. The",
      "audit-fix loop and the unjudged ask are now PORTED and fire without autonomy;",
      "three remain script-only, and the cutover stays blocked until they follow.",
      "",
      "All eight rules now exist on BOTH sides — the cutover is no longer blocked by",
      "parity. Length became a trigger on Harald's own suggestion: it does not depend",
      "on wording, so the agent cannot walk past it by accident, which is exactly how",
      "the ten-phrase ask detector failed on its first live outing."
    ],
    "rules": {
      "anti_loop": {
        "bash": "present",
        "rust": "present",
        "what": "a second pass never blocks; the anti-wedge valve"
      },
      "audit_fix_loop": {
        "bash": "present",
        "rust": "present",
        "what": "3+ refusals with no checkpoint reached -> run the architect"
      },
      "unjudged_ask": {
        "bash": "absent",
        "rust": "absent",
        "what": "RETIRED v4.0.0 (Harald, 2026-09-03: 'The communicator is annoying. I see the same output twice... Can we instead add a ruling'). It required a fresh-context reviewer to read every decision-class message before it was sent. What it caught was a checklist — an unopenable reference, a count with no object, an undefined term — and a checklist is a ruling, not a subagent; what it cost was the whole readback rendered to him beside the message it judged. The check now lives in the deployed instructions and in decision_test_length's own reason. The mechanical residual — length, undefined terms — stays, because both are decidable from the text with nothing to consult."
      },
      "unreported_degradation": {
        "bash": "absent",
        "rust": "present-but-inert",
        "what": "a tool result stamped DEGRADED: was consumed and the message does not say so (studio#4) — INERT until jawata-mcp#12 stamps responses: no producer emits a line beginning DEGRADED: today, so the rule is correct and unreachable"
      },
      "communicator_rule_a": {
        "bash": "absent",
        "rust": "absent",
        "what": "MERGED into unjudged_ask on 2026-08-20. It existed as the unconditional form of the same rule for one afternoon; an architect audit ruled that form a category error (an interception requirement on an observe-only hook API), and the scope returned to decision-class. No separate rule remains."
      },
      "nothing_armed_rule_b": {
        "bash": "absent",
        "rust": "present",
        "what": "Rule B: autonomy granted and nothing armed. LIVE since v3.14.1, and no longer inert — the grant reaches it from autonomy.rs. v4.0.0 replaced its exemption: a stop is cleared by the autocontinue seat's VERDICT: RESERVED, read out of the harness's tool-result record, and never by the agent writing a DECISION: line of its own."
      },
      "autocontinue_judge": {
        "bash": "absent",
        "rust": "present",
        "what": "v4.0.0. Under a live grant, a stop with nothing armed is put to a fresh-context subagent that reads the transcript and the plan ITSELF and answers RESERVED or RESOLVABLE. It is a bounce-and-verify rule, so it shares Rule B's marker rather than owning one — the block that demands the seat, and the block that carries the seat's next action, are both RULE B outcomes. Bound: the empty-turn ceiling, which the judge deliberately does not reset."
      },
      "seat_discipline": {
        "bash": "present",
        "rust": "present",
        "what": "a seat invoked with no gate call after it"
      },
      "decision_test_length": {
        "bash": "present",
        "rust": "present",
        "what": "a shaped message over the length budget"
      },
      "undefined_abbreviations": {
        "bash": "present",
        "rust": "present",
        "what": "undefined terms in a shaped message"
      }
    }
  },
  "role_generations": {
    "_why": [
      "Which GENERATION is live per role: the binary, or the generation-2 script.",
      "The stop_rules parity discipline, applied at role granularity — a role cuts",
      "over to the binary only when the binary holds parity, and a drop is declared",
      "here, never discovered.",
      "",
      "3.7.2 dogfood F4: the observer cut over by FILE EXISTENCE (the invocation",
      "path preferred any binary on disk), and the observer binary is a stub — it",
      "records a silence row and nothing else — while the script generation captures",
      "tool outcomes and the jawata-fallback audit trail. outcomes.log froze the",
      "moment the observer entry first pointed at the binary. Enforcement, stated exactly (audit F3 — the previous sentence here claimed more",
      "than was true): the STUDIO asserts the deploy's live/retired binary lists match these rows (the_deployed_role_list test), and the HOOK crate asserts every declared role resolves in its dispatch table with a known generation (role_generations_match_the_dispatch). This section governs the CLAUDE-CODE deploy only — see _scope; the Cursor deploy is all script-generation by construction, and a studio test pins that no Cursor entry names a role binary.",
      "declared binary here, and keeps the script for the rest.",
      "",
      "3.7.3 dogfood F5: the guard binary held parity on the shell-command half",
      "(java-grep redirect, jawata-fallback escape) and silently dropped the OTHER",
      "half — the .java hand-edit gate with its jawata-author authoring windows.",
      "Proven by a front-door Edit of a .java file that went through unblocked.",
      "The guard reverts to its script (which carries both halves) until the port."
    ],
    "_scope": "claude-code, and — since Sprint 28a — the rows govern Cursor too. Before that, Cursor was all script-generation by construction and a studio test pinned that no Cursor entry named a role binary. That test now checks the weaker, truer thing: a Cursor entry may name a role binary only where a row here declares that role live as a binary (a `cursor` key on a row may override `live` where the two clients legitimately diverge, as the observer's did from v3.7.9 until 28b D8; no row carries one today).",
    "primer": {
      "live": "binary"
    },
    "userprompt": {
      "live": "binary"
    },
    "recall": {
      "live": "binary"
    },
    "guard": {
      "live": "binary",
      "ported": "Sprint 28a (2026-08-12). The .java hand-edit gate moved into the binary: tool_name extraction, Edit/Write/MultiEdit denial, the session-scoped jawata-author window (TTL-bounded, so it cannot become a standing bypass), and the new-file allowance. BOTH halves are pinned by tests that SPAWN THE REAL BINARY — edit_gate_runs_the_real_binary.rs — including a regression asserting the shell half still denies a java grep, because the two previous cutovers each shipped one half. Windows forced the timing: a .sh cannot execute there, Cursor runs this role failClosed, and a hook that never returns BLOCKS the command — observed live on 2026-08-12 as an interactive bash window hung on cat."
    },
    "observer": {
      "live": "binary",
      "ported": "Sprint 28b D8 (2026-08-17). Both script halves are in the binary (observer.rs): outcome capture — the outcomes.log signals (slip, read-ungrounded, verify) — and the jawata-fallback audit trail — the slip store bridge plus the steering answer — with the C7 edit feed alongside. Pinned by the observer tests that drive the real module. Live on BOTH clients from this one row: Cursor has run the observer binary since v3.7.9 (its script did nothing, so there was no parity to lose — a cursor override on this row recorded that while the clients diverged), and Claude Code flips here, closing the 3.7.2 revert (outcomes.log froze the moment the entry pointed at the stub) the way 28a closed the guard's 3.7.3 revert."
    },
    "stop": {
      "live": "binary"
    }
  }
}
